[
  {
    "name": "redis-default-self",
    "engine": "redis",
    "connection_params": {},
    "collection_params": {
    },
    "search_params": [
      { "parallel": 1, "config": { "EF": 64 } },
      { "parallel": 100, "config": { "EF": 64 } }
    ],
    "upload_params": { "parallel": 16, "batch_size": 1024 }
  }
]
